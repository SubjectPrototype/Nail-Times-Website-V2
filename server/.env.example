PORT=4000
CLIENT_ORIGIN=http://localhost:3000
APP_PUBLIC_URL=http://localhost:3000
BOOKING_CANCEL_TOKEN_TTL=30d

MONGODB_URI=<paste your Atlas URI here>
JWT_SECRET=replace_with_a_long_random_secret

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD_HASH=replace_with_bcrypt_hash
ADMIN_2FA_ENABLED=false
DEFAULT_APPOINTMENT_MINUTES=60

# Shared PIN for workers using the /giftcard transaction page.
GIFTCARD_WORKER_PIN=replace_with_a_private_worker_pin

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WEBHOOK_BASE_URL=

RESEND_API_KEY=
FROM_EMAIL="Nail Shop <onboarding@resend.dev>"
ADMIN_NOTIFY_EMAIL=owner@example.com

# Shared secret used by the salon-side receipt printer bridge.
PRINT_BRIDGE_TOKEN=replace_with_a_long_random_secret
